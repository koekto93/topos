import React from "react";

import ModalDialog from "../../ModalDialog";

const modalName = "Callback";

const Callback = () => (
  <ModalDialog
  /* isOpen={isOpen}
    closeClick={closeModal}
    isMobile={isMobile}
    isConfirmForm={isMobile}
    modalName={modalName} */
  >
    <div>ddd</div>
  </ModalDialog>
);

export default Callback;
